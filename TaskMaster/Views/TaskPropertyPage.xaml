<UserControl x:Class="TaskMaster.Views.TaskPropertyPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskMaster.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl>
            <TabItem Header="Общие">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Триггеры">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
            <TabItem Header="Действия">
                <Grid Background="#FFE5E5E5"/>
            </TabItem>
        </TabControl>
        <StackPanel Orientation="Horizontal" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right">
            <Button Content="OK" Margin="10" Padding="10,5"/>
            <Button Content="Отмена" Margin="10" Padding="10,5"/>
        </StackPanel>
    </Grid>
</UserControl>
